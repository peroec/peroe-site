# ─────────────────────────────────────────────
# 大前端主站构建期环境变量模板（Vite 注入）
# 复制为 .env 后按需填写；.env 不入库
# ─────────────────────────────────────────────

# 论坛模块挂载路径（main-site 缝合 2xss_bbs 用）
# forum-bbs 的 withBase/absUrl 据此拼 /forum 前缀（登录回跳、GitHub OAuth 回调）
# main-site 主体路由不受影响（不使用 BASE_PATH）
VITE_BASE_PATH=/forum

# 论坛 API 回源（forum-bbs/lib/forum/api/client.ts 读取）
VITE_FORUM_API_BASE=https://forum.060730.xyz

# SEO 覆盖项（可选；通常使用 app/lib/site.config.json）
# VITE_SITE_URL=
# VITE_OG_IMAGE=
# VITE_AUTH_ALLOWED_ORIGINS=

# 部署用（不要提交真实值，wrangler secret put 或终端环境变量提供）
# CLOUDFLARE_API_TOKEN=
